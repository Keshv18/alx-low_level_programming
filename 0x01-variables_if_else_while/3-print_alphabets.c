#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
        char letter[] = "abcdefghijklmnopqrstuvwyzABCDEFGHIJKLMNOPQRSTUVWYZ";
        int b;

        for (b = 0; b <= 51; b++);
        {
                putchar(letters[b]);
        }
        putchar('\n');
        return (0);
}

